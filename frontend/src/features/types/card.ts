export interface Card {
    id: number
    cardNumber: string
    expirationDate: string
    cvv: string
    cardType: string
    cardNetwork: string
    accountType: string
    status: string
    limit: number
    createdAt: string
    updatedAt: string
}
